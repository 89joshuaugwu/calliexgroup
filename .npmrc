# @react-three/fiber v10 pins a tight react peer range (>=19 <19.3).
# legacy-peer-deps avoids npm ERESOLVE fights when drei / three sub-deps
# lag a patch behind — same pattern used across the buildwithzaza R3F setup.
legacy-peer-deps=true
